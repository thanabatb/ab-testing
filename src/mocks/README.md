# Mock Data

Keep fake JSON data in this folder.

Example:

- `contacts.json`
- `appointments.json`
- `products.json`

You can bootstrap flow testing by:

1. Reading seed data from JSON
2. Saving updates to `localStorage`
3. Adding fake latency and random error simulation in service functions
